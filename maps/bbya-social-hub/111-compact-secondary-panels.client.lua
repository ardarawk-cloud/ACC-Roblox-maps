-- BBYA MUSIC UI TEST — RETIRED COMPACT/MENU OVERRIDE
-- No runtime behavior. UI KERNEL v1 is the sole shell geometry authority.
-- PR synchronize trigger after batched kernel rebuild; intentionally inert.
print("[BBYA TEST] compact/menu override retired")
