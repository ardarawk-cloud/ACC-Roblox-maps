name: After School City Publish V1.1.3 Skate Mission Completion

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/after-school-city-publish-v113-skate-mission-completion.yml'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: after-school-city-live-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SOURCE_REPO: ardarawk-cloud/ACC-Roblox-maps
      SOURCE_SHA: 35ad050a3561121bca1f97310c51c282e893fd11
      BASE_LIVE_SHA: 0df86eebafec37054a26db434b5d46c37e2fee2c
      QC_RUN_ID: '33661112319'
      QC_ARTIFACT_ID: '9858715224'
      QC_ARTIFACT_NAME: after-school-city-v113-qc-35ad050a3561121bca1f97310c51c282e893fd11
      UNIVERSE_ID: '10745359869'
      PLACE_ID: '121603385909425'
      EXPECTED_BYTES: '184360'
      EXPECTED_SHA256: 358505300e8bf3c06f811bd2bb6bbcc9fe4fd6d7e4a45d9dbff9a9bbd6120def
      ROBLOX_API_KEY: ${{ secrets.ROBLOX_OPEN_CLOUD_MASTER_V2 }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout publisher repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify previous LIVE authority
        shell: bash
        run: |
          set -euo pipefail
          receipt='deploy-status/after-school-city-publish-skatepark-light-clearance-v1-1-2.json'
          [[ -f "$receipt" ]] || { echo 'V1.1.2 LIVE receipt missing'; exit 7; }
          [[ "$(jq -r '.status' "$receipt")" == 'LIVE_PUBLISHED' ]] || { echo 'Previous receipt is not LIVE_PUBLISHED'; exit 8; }
          [[ "$(jq -r '.sourceCommit' "$receipt")" == "$BASE_LIVE_SHA" ]] || { echo 'Previous LIVE source mismatch'; exit 9; }
          [[ "$(jq -r '.versionNumber' "$receipt")" == '48' ]] || { echo 'Previous LIVE version is not v48'; exit 10; }
          [[ "$(jq -r '.placeId' "$receipt")" == "$PLACE_ID" ]] || { echo 'Previous LIVE Place ID mismatch'; exit 11; }
          [[ "$(jq -r '.universeId' "$receipt")" == "$UNIVERSE_ID" ]] || { echo 'Previous LIVE Universe ID mismatch'; exit 12; }
          echo 'PREVIOUS LIVE AUTHORITY PASS v48'

      - name: Verify exact QC run and artifact authority
        shell: bash
        run: |
          set -euo pipefail
          [[ -n "${ROBLOX_API_KEY:-}" ]] || { echo 'ROBLOX_OPEN_CLOUD_MASTER_V2 is unavailable'; exit 13; }
          api='https://api.github.com/repos/ardarawk-cloud/ACC-Roblox-maps'
          curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H 'X-GitHub-Api-Version: 2022-11-28' "$api/actions/runs/$QC_RUN_ID" -o "$RUNNER_TEMP/qc-run.json"
          [[ "$(jq -r '.status' "$RUNNER_TEMP/qc-run.json")" == 'completed' ]] || { echo 'QC run is not completed'; exit 14; }
          [[ "$(jq -r '.conclusion' "$RUNNER_TEMP/qc-run.json")" == 'success' ]] || { echo 'QC run is not SUCCESS'; exit 15; }
          [[ "$(jq -r '.head_sha' "$RUNNER_TEMP/qc-run.json")" == "$SOURCE_SHA" ]] || { echo 'QC source SHA mismatch'; exit 16; }
          [[ "$(jq -r '.path' "$RUNNER_TEMP/qc-run.json")" == '.github/workflows/after-school-city-v113-skate-mission-completion-qc.yml' ]] || { echo 'Unexpected QC workflow'; exit 17; }
          curl -fsSL -H "Authorization: Bearer $GH_TOKEN" -H 'X-GitHub-Api-Version: 2022-11-28' "$api/actions/runs/$QC_RUN_ID/artifacts" -o "$RUNNER_TEMP/qc-artifacts.json"
          count=$(jq --argjson id "$QC_ARTIFACT_ID" --arg n "$QC_ARTIFACT_NAME" --arg s "$SOURCE_SHA" '[.artifacts[] | select(.id == $id and .name == $n and .expired == false and .workflow_run.head_sha == $s)] | length' "$RUNNER_TEMP/qc-artifacts.json")
          [[ "$count" == '1' ]] || { cat "$RUNNER_TEMP/qc-artifacts.json"; echo 'Exact QC artifact authority not found'; exit 18; }
          echo "QC AUTHORITY PASS run=$QC_RUN_ID artifact=$QC_ARTIFACT_ID source=$SOURCE_SHA"

      - name: Checkout exact V1.1.3 source for lock verification
        uses: actions/checkout@v4
        with:
          repository: ardarawk-cloud/ACC-Roblox-maps
          ref: 35ad050a3561121bca1f97310c51c282e893fd11
          path: asc-source
          fetch-depth: 0

      - name: Verify exact source and narrow V1.1.3 delta
        shell: bash
        run: |
          set -euo pipefail
          actual=$(git -C asc-source rev-parse HEAD)
          [[ "$actual" == "$SOURCE_SHA" ]] || { echo "Source SHA mismatch: $actual"; exit 20; }
          git -C asc-source cat-file -e "${BASE_LIVE_SHA}^{commit}"
          mapfile -t files < <(git -C asc-source diff --name-only "$BASE_LIVE_SHA" "$SOURCE_SHA")
          [[ ${#files[@]} -eq 4 ]] || { printf '%s\n' "${files[@]}"; echo "Unexpected delta count ${#files[@]}"; exit 21; }
          expected=(
            '.github/workflows/after-school-city-v113-skate-mission-completion-qc.yml'
            'maps/after-school-city/after-school-city.activity-config.lua'
            'maps/after-school-city/after-school-city.activities.server.lua'
            'maps/after-school-city/after-school-city.activities.client.lua'
          )
          for f in "${expected[@]}"; do printf '%s\n' "${files[@]}" | grep -Fxq "$f" || { echo "Missing expected delta $f"; exit 22; }; done
          cfg='asc-source/maps/after-school-city/after-school-city.activity-config.lua'
          server='asc-source/maps/after-school-city/after-school-city.activities.server.lua'
          client='asc-source/maps/after-school-city/after-school-city.activities.client.lua'
          grep -Fq '1.1.3-skate-mission-completion-1' "$cfg"
          grep -Fq 'RequireSkateboard = true' "$cfg"
          grep -Fq 'createBoardModel' "$server"
          grep -Fq 'ASC_SkateboardActive' "$server"
          grep -Fq 'SkateboardActive = cfg.RequireSkateboard == true' "$server"
          grep -Fq 'Target = checkpoints[1]' "$server"
          grep -Fq 'Target = session.Checkpoints[session.Step]' "$server"
          grep -Fq 'showCheckpointMarker' "$client"
          grep -Fq 'ASC_ActiveSkateCheckpoint_Local' "$client"
          [[ -z "$(git -C asc-source diff --name-only "$BASE_LIVE_SHA" "$SOURCE_SHA" -- maps/after-school-city/default.project.json maps/after-school-city/after-school-city.world.server.lua maps/after-school-city/after-school-city.streetlight-geometry-hotfix-v111.server.lua maps/after-school-city/after-school-city.skatepark-light-clearance-v112.server.lua maps/after-school-city/after-school-city.gameplay-service.lua maps/after-school-city/after-school-city.gameplay-config.lua maps/after-school-city/after-school-city.gameplay-bootstrap.server.lua maps/after-school-city/after-school-city.gameplay-hud.client.lua maps/after-school-city/after-school-city.ui-glass-v102.client.lua maps/after-school-city/after-school-city.music-player.client.lua maps/after-school-city/after-school-city.music-config.lua maps/after-school-city/after-school-city.intro.client.lua maps/after-school-city/after-school-city.swimmable-pool.server.lua)" ]] || { echo 'Protected source changed'; exit 23; }
          echo "SOURCE LOCK PASS source=$actual delta=4-files scope=skate-mission-completion"

      - name: Download exact QC-locked artifact
        uses: actions/download-artifact@v4
        with:
          name: after-school-city-v113-qc-35ad050a3561121bca1f97310c51c282e893fd11
          path: ${{ runner.temp }}/asc-v113-qc
          github-token: ${{ github.token }}
          repository: ardarawk-cloud/ACC-Roblox-maps
          run-id: 33661112319

      - name: Verify QC receipt and exact RBXL bytes/SHA256
        shell: bash
        run: |
          set -euo pipefail
          receipt="$RUNNER_TEMP/asc-v113-qc/qc-receipt.txt"
          normalized="$RUNNER_TEMP/asc-v113-qc/qc-receipt-normalized.txt"
          [[ -f "$receipt" ]] || { find "$RUNNER_TEMP/asc-v113-qc" -maxdepth 3 -type f -print; echo 'QC receipt missing'; exit 29; }
          tr -d '\r' < "$receipt" > "$normalized"
          grep -Fxq 'STATUS=QC_PASSED' "$normalized"
          grep -Fxq 'MODE=SKATE_MISSION_COMPLETION_V1_1_3' "$normalized"
          grep -Fxq "BASE_LIVE_COMMIT=$BASE_LIVE_SHA" "$normalized"
          grep -Fxq "SOURCE_COMMIT=$SOURCE_SHA" "$normalized"
          grep -Fxq "RBXL_BYTES=$EXPECTED_BYTES" "$normalized"
          grep -Fxq "SHA256=$EXPECTED_SHA256" "$normalized"
          grep -Fxq 'SKATEBOARD=SERVER_OWNED_AUTOMOUNT' "$normalized"
          grep -Fxq 'SKATE_VALIDATION=SERVER_REQUIRED' "$normalized"
          grep -Fxq 'CHECKPOINT_GUIDANCE=LOCAL_ACTIVE_ONLY' "$normalized"
          grep -Fxq 'CHECKPOINT_COUNT=3' "$normalized"
          grep -Fxq 'POOL_DELIVERY=UNCHANGED' "$normalized"
          grep -Fxq 'PERSISTENCE=UNCHANGED' "$normalized"
          grep -Fxq 'MUSIC=UNCHANGED' "$normalized"
          grep -Fxq 'MAP_LIGHTING_ROADS_DEDICATION=UNCHANGED' "$normalized"
          mapfile -t places < <(find "$RUNNER_TEMP/asc-v113-qc" -type f -name '*.rbxl' -print)
          [[ ${#places[@]} -eq 1 ]] || { find "$RUNNER_TEMP/asc-v113-qc" -maxdepth 3 -type f -print; echo "Expected exactly one RBXL, found ${#places[@]}"; exit 30; }
          place="${places[0]}"
          bytes=$(stat -c%s "$place")
          sha=$(sha256sum "$place" | awk '{print $1}')
          [[ "$bytes" == "$EXPECTED_BYTES" ]] || { echo "Byte mismatch: $bytes expected=$EXPECTED_BYTES"; exit 31; }
          [[ "$sha" == "$EXPECTED_SHA256" ]] || { echo "SHA256 mismatch: $sha expected=$EXPECTED_SHA256"; exit 32; }
          echo "PLACE_FILE=$place" >> "$GITHUB_ENV"
          echo "PLACE_BYTES=$bytes" >> "$GITHUB_ENV"
          echo "PLACE_SHA256=$sha" >> "$GITHUB_ENV"
          echo "EXACT QC ARTIFACT PASS bytes=$bytes sha256=$sha"

      - name: Publish V1.1.3 LIVE to Roblox
        shell: bash
        run: |
          set -euo pipefail
          url="https://apis.roblox.com/universes/v1/${UNIVERSE_ID}/places/${PLACE_ID}/versions?versionType=Published"
          success=0
          for attempt in 1 2 3 4; do
            response="$RUNNER_TEMP/asc-publish-response-$attempt.json"
            http=$(curl -sS --http1.1 --location --connect-timeout 30 --max-time 180 -o "$response" -w '%{http_code}' -X POST "$url" -H "x-api-key: $ROBLOX_API_KEY" -H 'Content-Type: application/octet-stream' --data-binary "@$PLACE_FILE" || true)
            body=$(cat "$response" 2>/dev/null || true)
            if [[ "$http" =~ ^2[0-9][0-9]$ ]]; then
              cp "$response" "$RUNNER_TEMP/asc-publish-response.json"
              echo "ROBLOX UPLOAD PASS attempt=$attempt http=$http"
              success=1
              break
            fi
            echo "ROBLOX UPLOAD attempt=$attempt HTTP $http: $body"
            if [[ "$body" != *'Request upload was interrupted before completion.'* ]]; then
              echo 'Non-transient Roblox publish error; stopping retries'
              exit 40
            fi
            sleep $((attempt * 3))
          done
          [[ "$success" == '1' ]] || { echo 'Roblox upload interrupted on all retry attempts'; exit 42; }
          body=$(cat "$RUNNER_TEMP/asc-publish-response.json")
          version=$(jq -r '.versionNumber // empty' "$RUNNER_TEMP/asc-publish-response.json")
          [[ -n "$version" && "$version" =~ ^[0-9]+$ ]] || { echo "Missing/invalid versionNumber: $body"; exit 41; }
          echo "PUBLISHED_VERSION=$version" >> "$GITHUB_ENV"
          echo "ROBLOX PUBLISH PASS version=$version"

      - name: Persist V1.1.3 LIVE receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p deploy-status
          receipt='deploy-status/after-school-city-publish-skate-mission-completion-v1-1-3.json'
          jq -n \
            --arg status 'LIVE_PUBLISHED' \
            --arg project 'AFTER SCHOOL CITY' \
            --arg mode 'SKATE_MISSION_COMPLETION_V1_1_3' \
            --arg sourceRepo "$SOURCE_REPO" \
            --arg sourceCommit "$SOURCE_SHA" \
            --arg baseLiveCommit "$BASE_LIVE_SHA" \
            --arg qcRunId "$QC_RUN_ID" \
            --arg qcArtifactId "$QC_ARTIFACT_ID" \
            --arg universeId "$UNIVERSE_ID" \
            --arg placeId "$PLACE_ID" \
            --arg versionNumber "$PUBLISHED_VERSION" \
            --arg rojoVersion '7.7.0' \
            --arg placeBytes "$PLACE_BYTES" \
            --arg placeSha256 "$PLACE_SHA256" \
            --arg runId "$GITHUB_RUN_ID" \
            --arg publishedAt "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            '{status:$status,project:$project,mode:$mode,sourceRepo:$sourceRepo,sourceCommit:$sourceCommit,baseLiveCommit:$baseLiveCommit,qcRunId:$qcRunId,qcArtifactId:($qcArtifactId|tonumber),universeId:$universeId,placeId:$placeId,versionNumber:($versionNumber|tonumber),rojoVersion:$rojoVersion,placeBytes:($placeBytes|tonumber),placeSha256:$placeSha256,runId:$runId,publishedAt:$publishedAt,note:"V1.1.3 exact QC-locked Skate Mission Completion pass. Skate Line now provides a server-owned auto-mounted skateboard, requires server-validated skateboard state for checkpoint progression, and shows only the active local checkpoint marker. Pool Laps, City Delivery, persistence, music, map, lighting, roads and dedication remain unchanged."}' > "$receipt"
          cat "$receipt"
          git config user.name 'After School City Publish Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -- "$receipt"
          git commit -m 'Record After School City V1.1.3 LIVE publish [skip ci]'
          for attempt in 1 2 3 4; do
            git pull --rebase origin main || { git rebase --abort || true; }
            if git push origin HEAD:main; then
              echo "Receipt push succeeded attempt=$attempt"
              exit 0
            fi
            echo "Receipt push retry attempt=$attempt"
            sleep $((attempt * 2))
          done
          echo 'Failed to persist V1.1.3 LIVE receipt after retries'
          exit 50
