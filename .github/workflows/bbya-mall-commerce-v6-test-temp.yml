name: BBYA Mall Commerce V6 Test

on:
  push:
    branches: [feat/bbya-mall-retail-density-v3-main]
    paths:
      - '.github/workflows/bbya-mall-commerce-v6-test-temp.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: [self-hosted, windows, x64]
    timeout-minutes: 15
    steps:
      - name: Fetch exact source
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $zip=Join-Path $env:RUNNER_TEMP 'mall-commerce-v6-source.zip'
          $dir=Join-Path $env:RUNNER_TEMP 'mall-commerce-v6-source'
          Remove-Item $dir -Recurse -Force -ErrorAction SilentlyContinue
          Invoke-WebRequest -Uri "https://codeload.github.com/ardarawk-cloud/ACC-Roblox-maps/zip/$env:GITHUB_SHA" -OutFile $zip
          Expand-Archive $zip -DestinationPath $dir -Force
          $repo=Get-ChildItem $dir -Directory | Select-Object -First 1
          if(-not $repo){throw 'Exact source missing'}
          Get-ChildItem $repo.FullName -Force | ForEach-Object { Copy-Item $_.FullName $env:GITHUB_WORKSPACE -Recurse -Force }
          Write-Host "EXACT_SOURCE=$env:GITHUB_SHA"
      - name: Static commerce v6 QC
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $client=Get-Content 'maps/bbya-social-hub/133-mall-robux-commerce.client.lua' -Raw
          foreach($m in @('COMMERCE CLIENT v6','ProductCarousel','ScrollingDirection=Enum.ScrollingDirection.X','Activated:Connect','PromptPurchase(player,id,true,Enum.CurrencyType.Default)','setMallHudHidden')){if(-not $client.Contains($m)){throw "Missing $m"}}
          Write-Host 'BBYA_MALL_COMMERCE_V6_STATIC_QC_PASS'
      - name: Enable third party sales in test build
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $p='maps/bbya-social-hub/default.project.json'
          $s=Get-Content $p -Raw
          $pattern='"Workspace"\s*:\s*\{\s*"\$className"\s*:\s*"Workspace"\s*\}'
          $replacement='"Workspace": {"$className":"Workspace","$properties":{"AllowThirdPartySales":true}}'
          $patched=[regex]::Replace($s,$pattern,$replacement,1)
          if($patched -eq $s){throw 'Workspace project block not patched'}
          $patched | Set-Content $p -Encoding UTF8
          if(-not ((Get-Content $p -Raw).Contains('"AllowThirdPartySales":true'))){throw 'AllowThirdPartySales missing'}
          Write-Host 'BBYA_TEST_THIRD_PARTY_SALES_ENABLED'
      - name: Build canonical test candidate
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $toolDir=Join-Path $env:RUNNER_TEMP 'rojo-7.7.0';New-Item -ItemType Directory -Force -Path $toolDir | Out-Null
          $rojo=Join-Path $toolDir 'rojo.exe'
          if(-not (Test-Path $rojo)){$z=Join-Path $env:RUNNER_TEMP 'rojo.zip';Invoke-WebRequest -Uri 'https://github.com/rojo-rbx/rojo/releases/download/v7.7.0/rojo-7.7.0-windows-x86_64.zip' -OutFile $z;Expand-Archive $z -DestinationPath $toolDir -Force}
          if((& $rojo --version) -notmatch '7\.7\.0'){throw 'Rojo 7.7.0 required'}
          $out=Join-Path $env:RUNNER_TEMP 'bbya-mall-commerce-v6-test.rbxl'
          & $rojo build 'maps/bbya-social-hub/default.project.json' -o $out
          if($LASTEXITCODE -ne 0 -or -not (Test-Path $out)){throw 'Build failed'}
          $bytes=(Get-Item $out).Length;$sha=(Get-FileHash $out -Algorithm SHA256).Hash.ToLowerInvariant()
          "PLACE_FILE=$out" | Out-File $env:GITHUB_ENV -Append
          "BUILD_BYTES=$bytes" | Out-File $env:GITHUB_ENV -Append
          "BUILD_SHA=$sha" | Out-File $env:GITHUB_ENV -Append
          Write-Host "BBYA_MALL_COMMERCE_V6_BUILD_PASS bytes=$bytes sha256=$sha source=$env:GITHUB_SHA"
      - name: Publish test place only
        shell: powershell
        env:
          ROBLOX_API_KEY: ${{ secrets.ROBLOX_OPEN_CLOUD_MASTER_V2 }}
        run: |
          $ErrorActionPreference='Stop'
          if([string]::IsNullOrWhiteSpace($env:ROBLOX_API_KEY)){throw 'MASTER_V2 missing'}
          $url='https://apis.roblox.com/universes/v1/10762005984/places/124607344716828/versions?versionType=Published'
          $resp=& curl.exe --silent --show-error --fail-with-body --http1.1 --ipv4 --request POST --header "x-api-key: $env:ROBLOX_API_KEY" --header 'Content-Type: application/octet-stream' --header 'Expect:' --data-binary "@$env:PLACE_FILE" $url
          if($LASTEXITCODE -ne 0){throw 'Test publish failed'}
          $r=$resp | ConvertFrom-Json
          if(-not $r.versionNumber){throw 'versionNumber missing'}
          Write-Host "BBYA_MALL_COMMERCE_V6_TEST_PUBLISHED version=$($r.versionNumber) source=$env:GITHUB_SHA build=$env:BUILD_SHA"
