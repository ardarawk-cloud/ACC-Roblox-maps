name: MOUNT BBYA — STOPPED / DO NOT PUBLISH

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stopped:
    runs-on: ubuntu-latest
    steps:
      - name: Project stop gate
        shell: bash
        run: |
          echo "MOUNT BBYA is STOPPED / DO NOT TOUCH."
          echo "Publishing is disabled by owner authority dated 2026-09-08."
