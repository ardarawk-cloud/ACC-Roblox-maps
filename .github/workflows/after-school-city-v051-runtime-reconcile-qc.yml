name: After School City V0.5.1 Runtime Reconcile QC

on:
  push:
    branches: [after-school-city/scaffold-v1]
    paths:
      - 'maps/after-school-city/**'
      - '.github/scripts/after-school-city-runtime-reconcile-audit.ps1'
      - '.github/workflows/after-school-city-v051-runtime-reconcile-qc.yml'
  pull_request:
    branches: [main]
    paths:
      - 'maps/after-school-city/**'
      - '.github/scripts/after-school-city-runtime-reconcile-audit.ps1'
      - '.github/workflows/after-school-city-v051-runtime-reconcile-qc.yml'

permissions:
  contents: read

jobs:
  qc:
    runs-on: [self-hosted, windows, x64]
    timeout-minutes: 15
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Cloud source spatial audit V4
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-spatial-audit.ps1'

      - name: Cloud source orientation audit V1
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-orientation-audit.ps1'

      - name: Cloud source visual fidelity audit V1
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-visual-fidelity-audit.ps1'

      - name: Cloud source runtime reconcile audit V1
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-runtime-reconcile-audit.ps1'

      - name: Runtime reconciliation authority locks
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $config = Get-Content 'maps/after-school-city/after-school-city.config.lua' -Raw
          $project = Get-Content 'maps/after-school-city/default.project.json' -Raw
          $runtime = Get-Content 'maps/after-school-city/after-school-city.runtime-reconcile.server.lua' -Raw
          $intro = Get-Content 'maps/after-school-city/after-school-city.intro.client.lua' -Raw
          $null = $project | ConvertFrom-Json
          foreach($token in @('RuntimeReconcileVersion = "0.5.1-runtime-reconcile-1"','EnableRuntimeReconcilePass = true','EnableActivities = false','EnableEconomy = false','EnablePersistence = false')){
            if($config -notmatch [regex]::Escape($token)){ throw "Config marker missing: $token" }
          }
          foreach($token in @('ASC_RuntimeReconcile','after-school-city.runtime-reconcile.server.lua')){
            if($project -notmatch [regex]::Escape($token)){ throw "Project marker missing: $token" }
          }
          foreach($token in @('V051_RuntimeReconcile','ASC_RuntimeOrderingRaceFixed','ASC_FinalVisualStateReconciled','REQUIRED_COMPLETION_ATTRIBUTES','waitForCompletionAttribute')){
            if($runtime -notmatch [regex]::Escape($token)){ throw "Runtime marker missing: $token" }
          }
          foreach($token in @('PUTU AZYA PUTRI','BINTANG HARDAJAYA','WITH LOVE, DAD','ENTER THE CITY')){
            if($intro -notmatch [regex]::Escape($token)){ throw "Dedication lock missing: $token" }
          }
          if($runtime -match 'MarketplaceService|PromptProductPurchase|ProcessReceipt|DataStoreService|BillboardGui'){ throw 'Forbidden authority/debug marker in Runtime Reconcile' }
          Write-Host 'AFTER SCHOOL CITY v0.5.1 runtime reconcile static QC PASS'

      - name: Prepare Rojo 7.7.0
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $cmd=Get-Command rojo -ErrorAction SilentlyContinue
          if($cmd){
            $rojoExe=$cmd.Source
          } else {
            $dir=Join-Path $env:RUNNER_TEMP 'asc-rojo-7.7.0-v051'
            $zip=Join-Path $env:RUNNER_TEMP 'asc-rojo-7.7.0-v051.zip'
            if(Test-Path $dir){Remove-Item $dir -Recurse -Force}
            New-Item -ItemType Directory -Path $dir -Force | Out-Null
            Invoke-WebRequest -UseBasicParsing -Uri 'https://github.com/rojo-rbx/rojo/releases/download/v7.7.0/rojo-7.7.0-windows-x86_64.zip' -OutFile $zip
            Expand-Archive -Path $zip -DestinationPath $dir -Force
            $exe=Get-ChildItem $dir -Filter rojo.exe -File -Recurse | Select-Object -First 1
            if(-not $exe){throw 'rojo.exe not found'}
            $rojoExe=$exe.FullName
          }
          $version=& $rojoExe --version
          Write-Host $version
          if($LASTEXITCODE -ne 0){throw 'Rojo version check failed'}
          if(($version -join ' ') -notmatch '7\.7\.0'){throw "Expected Rojo 7.7.0, got $version"}
          "ROJO_EXE=$rojoExe" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build exact V0.5.1 map
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $place=Join-Path $env:RUNNER_TEMP 'after-school-city-v051.rbxl'
          & $env:ROJO_EXE build 'maps/after-school-city/default.project.json' -o $place
          if($LASTEXITCODE -ne 0){throw "Rojo build failed: $LASTEXITCODE"}
          if(-not (Test-Path $place)){throw 'Generated RBXL missing'}
          $bytes=(Get-Item $place).Length
          if($bytes -lt 1024){throw "Generated RBXL suspiciously small: $bytes bytes"}
          $sha=(Get-FileHash -Algorithm SHA256 $place).Hash.ToLowerInvariant()
          Write-Host "AFTER SCHOOL CITY V0.5.1 ROJO BUILD PASS - $bytes bytes - sha256=$sha"
