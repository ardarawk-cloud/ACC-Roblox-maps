name: AeroClub Hangar v1.1 Visual LAB (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v1.1 publisher retired; AeroClub v1.2 owns LAB publishing"
